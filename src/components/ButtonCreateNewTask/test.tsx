import { fireEvent, render } from "@testing-library/react"
import React from "react"
import { ButtonCreateNewTask } from "."

describe("ButtonCreateNewTask", () => {
  it.todo("calls the provided click event handler when clicked")
})
